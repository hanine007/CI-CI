name: Node js CI
on :
  push :
    branches [main]
  pull_request :
  worflows_dispatch :
  schedule :
    - cron : '0 0 * * *'

jobs :
  build :
    strategy :
     matrix :
         node-version : [14, 16, 18]
         os : [ubuntu-latest, windows-latest, macos-latest]
    runs-on : ${{ matrix.os }}
    steps:
    -name : checkout
        uses : actions/checkout@v3
        -name : setup node
        uses : actions/setup-node@v3
        with :
        node-version : ${{matrix.node-version }}
        -name : install dependencies
        run : npm ci 
        -name : run tests
        run : npm test
        -name : upload  test 
        uses: actions/artifact@v3
        with :
        name : test-results with ${{ matrix.node-version }} and ${{ matrix.os }}
        path : test-results  






name: Node.js CI/CD Full Pipeline

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'  # Tous les jours à 2h

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node-version: [16.x, 18.x]
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run tests
        run: npm test
        
      - name: Upload test results
        uses: actions/upload-artifact@v3
        with:
          name: test-results-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: tests/
